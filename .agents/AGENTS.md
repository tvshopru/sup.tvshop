# Project Rules & Workflows

- Whenever making any edits or changes to the project files via chat, ALWAYS remind the user to run `git pull` locally (or refresh their manager panel `tvshop_manager`), so local and remote versions remain perfectly synchronized.
